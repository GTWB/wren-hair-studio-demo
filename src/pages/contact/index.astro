---
import Layout from "../../layouts/Layout.astro";
import ConsentCheckbox from "../../components/ConsentCheckbox.astro";
import { salon } from "../../data/site";
---

<Layout
  title="Contact"
  description="Get in touch with Wren Hair Studio in Clapham — find our address, phone number, opening hours and a contact form."
>
  <section class="mx-auto max-w-6xl px-5 sm:px-8 pt-14 sm:pt-20 pb-10">
    <p class="eyebrow text-accent text-xs sm:text-sm mb-4">Find us</p>
    <h1 class="font-display font-black uppercase text-4xl sm:text-5xl leading-[1.05] tracking-tight text-ink max-w-2xl">
      Get in <span class="text-accent">touch</span>
    </h1>
    <p class="mt-6 text-lg text-ink-soft max-w-xl leading-relaxed">
      Questions about a service, a colour change you're nervous about, or
      just want to check we've got space this week &mdash; send us a message
      and we'll reply personally.
    </p>
  </section>

  <section class="reveal mx-auto max-w-6xl px-5 sm:px-8 pb-24 grid gap-10 lg:grid-cols-12">
    <div class="lg:col-span-7 rounded-2xl bg-paper shadow-md p-6 sm:p-10">
      <!--
        Web3Forms: free form-backend service (no server/backend code needed).
        Submissions are emailed directly to the address tied to this access
        key. Access keys are meant to be used client-side like this — they
        are not secret credentials. Swap for the client's own Web3Forms
        access key when this template is reused for a real customer.
      -->
      <form
        id="contact-form"
        name="contact"
        method="POST"
        class="space-y-5"
      >
        <input type="hidden" name="access_key" value="200d2d09-2416-4066-9715-990c97589661" />
        <input type="hidden" name="subject" value="New enquiry from wrenhairstudio.netlify.app" />
        <input type="checkbox" name="botcheck" class="hidden" style="display:none" tabindex="-1" autocomplete="off" />

        <div class="grid sm:grid-cols-2 gap-5">
          <div>
            <label for="c-name" class="block text-sm text-ink-soft mb-1.5">Full name</label>
            <input id="c-name" name="name" type="text" required autocomplete="name"
              class="w-full rounded-lg border border-line bg-canvas px-4 py-2.5 text-ink focus-visible:outline-2 focus-visible:outline-accent" />
          </div>
          <div>
            <label for="c-phone" class="block text-sm text-ink-soft mb-1.5">Phone (optional)</label>
            <input id="c-phone" name="phone" type="tel" autocomplete="tel"
              class="w-full rounded-lg border border-line bg-canvas px-4 py-2.5 text-ink focus-visible:outline-2 focus-visible:outline-accent" />
          </div>
        </div>

        <div>
          <label for="c-email" class="block text-sm text-ink-soft mb-1.5">Email</label>
          <input id="c-email" name="email" type="email" required autocomplete="email"
            class="w-full rounded-lg border border-line bg-canvas px-4 py-2.5 text-ink focus-visible:outline-2 focus-visible:outline-accent" />
        </div>

        <div>
          <label for="c-message" class="block text-sm text-ink-soft mb-1.5">Message</label>
          <textarea id="c-message" name="message" rows="5" required
            class="w-full rounded-lg border border-line bg-canvas px-4 py-2.5 text-ink focus-visible:outline-2 focus-visible:outline-accent"></textarea>
        </div>

        <ConsentCheckbox id="contact-consent" name="consent" />

        <button
          type="submit"
          class="inline-flex items-center justify-center bg-ink text-canvas font-display font-bold uppercase tracked text-sm px-8 py-4 rounded-lg hover:bg-accent hover:text-ink transition-colors"
        >
          Send message
        </button>

        <p
          id="contact-confirmation"
          role="status"
          class="hidden rounded-lg border border-accent bg-canvas-alt text-ink px-5 py-4 text-sm"
        >
          Thanks &mdash; your message has been sent. We'll get back to you
          within one working day.
        </p>
        <p
          id="contact-error"
          role="alert"
          class="hidden rounded-lg border border-swatch-copper bg-canvas-alt text-ink px-5 py-4 text-sm"
        >
          Something went wrong sending your message. Please call us on
          {salon.phone} instead.
        </p>
      </form>
    </div>

    <div class="lg:col-span-5 space-y-6">
      <div class="rounded-2xl bg-canvas-alt shadow-md p-6">
        <p class="eyebrow text-accent text-xs mb-3">Studio</p>
        <p class="text-ink-soft leading-relaxed">
          {salon.addressLine1}<br />
          {salon.addressLine2}
        </p>
        <p class="mt-4 text-ink-soft">
          <a class="hover:text-accent" href={salon.phoneHref}>{salon.phone}</a><br />
          <a class="hover:text-accent" href={`mailto:${salon.email}`}>{salon.email}</a>
        </p>
        <div class="flex gap-5 mt-4 font-display text-sm uppercase tracked">
          <a class="text-accent hover:text-ink" href={salon.instagram} target="_blank" rel="noopener noreferrer">Instagram</a>
          <a class="text-accent hover:text-ink" href={salon.facebook} target="_blank" rel="noopener noreferrer">Facebook</a>
        </div>
      </div>

      <div class="rounded-2xl bg-canvas-alt shadow-md p-6">
        <p class="eyebrow text-accent text-xs mb-3">Opening hours</p>
        <ul class="text-sm text-ink-soft space-y-1.5">
          {
            salon.hours.map((h) => (
              <li class="flex justify-between gap-4">
                <span>{h.day}</span>
                <span>{h.time}</span>
              </li>
            ))
          }
        </ul>
      </div>

      <!-- Static map embed placeholder — no API key required. Swap for a
           live Google Maps / OpenStreetMap embed with the client's exact
           address once confirmed. -->
      <div class="rounded-2xl overflow-hidden shadow-md">
        <iframe
          title="Map showing the approximate location of Wren Hair Studio on Abbeville Road, Clapham"
          src="https://www.openstreetmap.org/export/embed.html?bbox=-0.1425%2C51.4585%2C-0.1325%2C51.4635&layer=mapnik&marker=51.4610%2C-0.1375"
          class="w-full h-64 border-0"
          loading="lazy"
        ></iframe>
      </div>
    </div>
  </section>
</Layout>

<script>
  const form = document.getElementById("contact-form") as HTMLFormElement | null;
  const confirmation = document.getElementById("contact-confirmation");
  const errorMsg = document.getElementById("contact-error");

  form?.addEventListener("submit", async (e) => {
    e.preventDefault();
    errorMsg?.classList.add("hidden");

    const data = new FormData(form);
    const payload = Object.fromEntries(data.entries());

    try {
      // Web3Forms documented AJAX pattern: JSON body, Content-Type
      // application/json only (no extra headers — Web3Forms' CORS allowance
      // is sensitive to this). No backend of our own required.
      const response = await fetch("https://api.web3forms.com/submit", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(payload),
      });
      const result = await response.json();
      if (result.success) {
        confirmation?.classList.remove("hidden");
        form.reset();
      } else {
        errorMsg?.classList.remove("hidden");
      }
    } catch {
      errorMsg?.classList.remove("hidden");
    }
    confirmation?.scrollIntoView({ behavior: "smooth", block: "nearest" });
  });
</script>
